# Local development dependencies go here
-r base.txt
# coverage==3.7.1
# Sphinx
django-extensions==1.5.5
# Werkzeug==0.10.4

# django-debug-toolbar that works with Django 1.5+
django-debug-toolbar==1.3.0

# adds CORS (Cross-Origin Resource Sharing) headers to responses, util for testing the app on browser
django-cors-headers
